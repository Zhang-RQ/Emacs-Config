(global-set-key (kbd "C-t") 'neotree-toggle)

(global-set-key (kbd "C-o") 'smart-compile)

(global-set-key (kbd "<C-tab>") 'self-insert-command)

; (defun PrintStupid ()
;   (interactive)
;   (message "Don't use arrow bottom!"))

; (global-set-key (kbd "<right>") 'PrintStupid)

; (global-set-key (kbd "<left>") 'PrintStupid)

; (global-set-key (kbd "<up>") 'PrintStupid)

; (global-set-key (kbd "<down>") 'PrintStupid)
